FROM pgduckdb/pgduckdb:17-v1.1.1

# PostgreSQL 17 with pg_duckdb extension (pre-built)
# Official image from pgduckdb project: https://hub.docker.com/r/pgduckdb/pgduckdb
# Includes pg_duckdb extension ready to use without additional compilation
