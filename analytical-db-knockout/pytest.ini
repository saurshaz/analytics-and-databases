[pytest]
minversion = 7.0
testpaths = benchmarks
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    benchmark: mark test as a benchmark suite
    validation: mark test as a validation/correctness test
    slow: mark test as slow running
addopts = -v -s --tb=short
